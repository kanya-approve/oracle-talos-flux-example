---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: profilarr
  namespace: apps
spec:
  chart:
    spec:
      chart: profilarr
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: truecharts
  interval: 50m
  values:
    common:
      ingress:
        main:
          integrations:
            traefik:
              enabled: false
    persistence:
      config:
        size: 50Mi
        type: emptyDir
    workload:
      main:
        podSpec:
          containers:
            main:
              env:
                HOME: /tmp
