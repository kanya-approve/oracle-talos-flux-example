---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sabnzbd
  namespace: apps
spec:
  chart:
    spec:
      chart: sabnzbd
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: truecharts
  interval: 50m
  values:
    common:
      ingress:
        main:
          integrations:
            traefik:
              enabled: false
    portal:
      open:
        enabled: false
    persistence:
      config:
        size: 50Mi
        type: emptyDir
